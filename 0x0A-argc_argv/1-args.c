#include <stdio.h>

/*
 * main - prints the number of arguments passed into it.
 * @argc: number of command line arguments
 * Return: 0 - Success
 */

int main(int argc);
{
	int i;
	printf("argc = %d\n", argc);
	return 0;
}
