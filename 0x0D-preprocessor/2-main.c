#include <stdio.h>
#define __FILE__

/**
 * main - prints the name of the file it was compiled from, followed by a new line
 * Return: Always 0
 */

int main(void)
{
	printf("File :%s\n", __FILE__ );
}
return (0);
