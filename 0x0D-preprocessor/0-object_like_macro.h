#include <stdio.h>

#define SIZE 1024

/**
 * main - defines a macro named SIZE as an abbreviation for the token 1024
 * Return: 0
 */

int main(void)
{
	int n = SIZE
		printf("%d",n);
	return (0);
}
